{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.unero.com/events/PasswordResetRequested.schema.json",
  "title": "PasswordResetRequested payload",
  "description": "Emitted by identity-service when a password reset is requested. Carries an opaque delivery_ref (not a secret) that the notification service exchanges — authenticated — for the minted token. No email, token, or hash.",
  "type": "object",
  "required": ["user_id", "delivery_ref", "occurred_at"],
  "additionalProperties": false,
  "properties": {
    "user_id": { "$ref": "https://schemas.unero.com/common.schema.json#/$defs/Ulid" },
    "delivery_ref": { "type": "string", "minLength": 16 },
    "occurred_at": { "$ref": "https://schemas.unero.com/common.schema.json#/$defs/Timestamp" }
  }
}
