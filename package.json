{
  "name": "@uneroplatform/shared-schemas",
  "version": "1.0.0",
  "description": "Shared JSON Schemas, event contracts, and OpenAPI components for the Unero platform.",
  "license": "UNLICENSED",
  "private": true,
  "type": "module",
  "files": ["schemas", "openapi", "examples"],
  "scripts": {
    "validate": "node tools/validate.mjs",
    "lint:openapi": "npx -y @stoplight/spectral-cli@6 lint \"openapi/**/*.yaml\" --ruleset .spectral.yaml",
    "check": "npm run validate && npm run lint:openapi",
    "test": "npm run validate"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
