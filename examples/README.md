# examples/

Canonical valid instances. CI validates each example against its schema (envelope for
events, error-envelope for errors), so these double as living documentation and as
regression fixtures for consumers.
