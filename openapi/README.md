# openapi/components

Reusable OpenAPI 3.1 components every service spec `$ref`s instead of redefining. `Money`
and `Error` are the two most-shared; `responses/common.yaml` gives the standard error
responses. A change here is a versioned, reviewed, org-wide change (see VERSIONING.md).
